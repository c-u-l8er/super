import test from 'node:test';import assert from 'node:assert/strict';
import {taskEditorView,canOpenTaskEditor} from '../cockpit/ui/task-editor.js';
const task={id:'t',revision:2,status:'planned'},p={},context={task:{id:'t',revision:2,world:'world'},root:'/repo',session:'session',generation:4,busy:false,files:[{path:'a.js',unsaved:true,selected:true}]},request={task:context.task,session:'session',generation:4,path:'a.js'};
test('matching context exposes draft state without asserting saved or tested bytes',()=>{const v=taskEditorView(p,task,'world',context);assert.equal(v.state,'linked');assert.equal(v.files[0].unsaved,true);assert.equal(canOpenTaskEditor(p,task,'world',context,request),true);});
test('unavailable, foreign and stale task context reveals no tabs',()=>{for(const [projection,t,world,c] of [[null,task,'world',context],[p,task,'foreign',context],[p,{...task,id:'other'},'world',context],[p,{...task,revision:3},'world',context],[p,{...task,status:'completed'},'world',context],[p,task,'world',null]]){assert.deepEqual(taskEditorView(projection,t,world,c).files,[]);assert.equal(canOpenTaskEditor(projection,t,world,c,request),false);}});
test('replaced repository, closed tab, old session and busy editor refuse navigation',()=>{for(const c of [{...context,generation:5},{...context,session:'new'},{...context,files:[]},{...context,busy:true}])assert.equal(canOpenTaskEditor(p,task,'world',c,request),false);for(const r of [{...request,path:'missing'},{...request,task:{...request.task,revision:1}},{...request,task:{...request.task,id:'other'}}])assert.equal(canOpenTaskEditor(p,task,'world',context,r),false);});
